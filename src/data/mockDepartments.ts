import { Department, DEFAULT_DEPARTMENTS } from "@/types/departments";

// Shared departments data - this is the single source of truth for all departments
export const mockDepartments: Department[] = [...DEFAULT_DEPARTMENTS];
